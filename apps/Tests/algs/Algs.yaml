initExp:
  rets:
    type: bool
    values: true

getQuery:
  rets:
    type: bool
    values: true

processAnswer:
    rets:
      type: bool
      values: true

getModel:
  rets:
    type: bool
    values: true
